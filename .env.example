# ScrAI environment configuration template

# Default runtime settings live in config/settings.toml.
# Use env vars only for secrets or per-environment overrides.

# Google Cloud / Firestore
GOOGLE_CLOUD_PROJECT=
GOOGLE_APPLICATION_CREDENTIALS=
SCRAI_FIRESTORE_PROJECT_ID=
SCRAI_FIRESTORE_CREDENTIALS=

# LLM providers
OPENROUTER_API_KEY=
# SCRAI_LLM_PRIMARY_PROVIDER=openrouter

# Local proxy example (optional)
SCRAI_LLM_PRIMARY_PROVIDER=lm_proxy
